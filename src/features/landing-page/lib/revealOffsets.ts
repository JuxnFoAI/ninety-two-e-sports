/** Reveal slots for a full section header (title, description). */
export const SECTION_HEADER_REVEAL_COUNT = 2;

/** Reveal slots for a compact header (title only) without description. */
export const SECTION_COMPACT_HEADER_REVEAL_COUNT = 1;

/** First reveal index after a compact header and one control row (e.g. division tabs). */
export const SECTION_COMPACT_HEADER_WITH_CONTROLS_REVEAL_START =
  SECTION_COMPACT_HEADER_REVEAL_COUNT + 1;
