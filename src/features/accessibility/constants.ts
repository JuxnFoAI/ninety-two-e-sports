/**
 * Applied directly on each visual layer (not a shared parent) so `position: fixed`
 * backgrounds keep viewport sizing when color-blind filters are active.
 */
export const A11Y_COLOR_FILTER_LAYER_CLASS = "a11y-color-filter-layer";
